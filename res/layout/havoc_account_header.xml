<?xml version="1.0" encoding="utf-8"?>
<!--
  Havoc OS Settings Homepage — Account Header + Search Bar
  Polished: better spacing, improved avatar sizing, responsive margins,
  inter-element breathing room, refined text hierarchy
  -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="6dp"
    android:background="@color/havoc_bg_primary">

    <!-- ════════ Account Card ════════ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/havoc_tile_bg_ripple"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="14dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:orientation="horizontal">

        <!-- Avatar — tap to switch Google accounts -->
        <ImageView
            android:id="@+id/havoc_account_avatar"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:background="@drawable/havoc_avatar_bg"
            android:src="@drawable/ic_account_circle_24dp"
            android:scaleType="centerCrop"
            android:clipToOutline="true"
            android:layout_marginEnd="14dp"
            android:contentDescription="@string/account_settings_title" />

        <!-- Name + Email text column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginEnd="8dp">

            <TextView
                android:id="@+id/havoc_account_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/havoc_text_primary"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.01"
                android:singleLine="true"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/havoc_account_email"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:textSize="13sp"
                android:textColor="@color/havoc_text_secondary"
                android:fontFamily="sans-serif"
                android:singleLine="true"
                android:ellipsize="end" />
        </LinearLayout>

        <!-- Search icon button -->
        <ImageView
            android:id="@+id/havoc_search_icon"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:padding="7dp"
            android:src="@android:drawable/ic_menu_search"
            android:tint="@color/havoc_text_primary"
            android:scaleType="centerInside"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/search_settings"
            android:layout_marginEnd="2dp" />

        <!-- QR code icon button -->
        <ImageView
            android:id="@+id/havoc_qr_icon"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:padding="7dp"
            android:src="@drawable/ic_qrcode_24dp"
            android:tint="@color/havoc_text_primary"
            android:scaleType="centerInside"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/havoc_qr_code" />
    </LinearLayout>

    <!-- ════════ Search Bar ════════ -->
    <LinearLayout
        android:id="@+id/havoc_search_bar"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/havoc_search_bg"
        android:gravity="center_vertical"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:orientation="horizontal"
        android:clickable="true"
        android:focusable="true">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@android:drawable/ic_menu_search"
            android:tint="@color/havoc_text_tertiary"
            android:scaleType="centerInside"
            android:layout_marginEnd="14dp"
            android:importantForAccessibility="no" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/search_settings"
            android:textSize="15sp"
            android:textColor="@color/havoc_text_tertiary"
            android:fontFamily="sans-serif" />
    </LinearLayout>

</LinearLayout>
